# Core pipeline + dashboard. Deployment extras live in requirements-deploy.txt.
#
# Lower bounds are the versions this project is developed and verified against.
# numpy>=2.0 is a hard requirement: the evaluation metrics use np.trapezoid,
# which replaced the removed np.trapz.

# ── Core scientific stack ──────────────────────────────────────────────────────
numpy>=2.0
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

# ── Gradient boosting ─────────────────────────────────────────────────────────
xgboost>=2.1

# ── Causal ML ─────────────────────────────────────────────────────────────────
econml>=0.16            # T/S/X-Learner, DRLearner
scikit-uplift>=0.5      # sklift: qini_curve, qini_auc_score

# ── Data / SQL ────────────────────────────────────────────────────────────────
duckdb>=1.0
pyarrow>=16             # parquet I/O

# ── Visualisation / Dashboard ─────────────────────────────────────────────────
matplotlib>=3.9
plotly>=5.22
streamlit>=1.36

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyterlab>=4.2
nbconvert>=7.16         # scripts/run_pipeline.py executes the notebooks
nbformat>=5.10

# ── Dev / quality ─────────────────────────────────────────────────────────────
black>=24
ruff>=0.5
pytest>=8
